package Strategy;

import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;

public class ClientCode {
    public static void main(String[] args) {
        //------------------------------------------------------------------------------


//
//        try (BufferedReader reader = new BufferedReader(new InputStreamReader(System.in))) {
//            inputType = Integer.parseInt(reader.readLine());
//        } catch (IOException e) {
//            throw new RuntimeException(e);
//        }
//
//        SearchAlgorithms searchAlgorithms = new SearchAlgorithms();
//        switch (inputType) {
//            case 1 -> searchAlgorithms.SearchByDistance();
//            case 2 -> searchAlgorithms.SearchByPrice();
//        }


        //----------------------------------------------------------------------------------------


//        SearchStrategy strategy = null;
//        int inputType;
//        System.out.println("Enter number of strategy");
//        try (BufferedReader reader = new BufferedReader(new InputStreamReader(System.in))) {
//            inputType = Integer.parseInt(reader.readLine());
//        } catch (IOException e) {
//            throw new RuntimeException(e);
//        }
//        switch (inputType) {
//            case 1 -> strategy = new SearchByDistanceStrategy();
//            case 2 -> strategy = new SearchByPriceStrategy();
//        }
//        if (strategy != null) strategy.execute();

        //--------------------------------------------------------------------------------------------------

    }
}
